import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    areaLink: {
        width: '100%',
        borderBottomWidth: 1,
        borderBottomColor: '#2d2d2d',
        flexDirection: 'row',
        gap: 15,
        alignItems: 'center',
        backgroundColor: '#fff',
        height: 60,
    },

    icon: {
        marginLeft: 15
    },

    label: {
        fontSize: 16,
        color: 'black',
    }
});